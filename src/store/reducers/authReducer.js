// takes in two parameters
// ! when we don't have a state we pass in initial state

const initState = {};
const authReducer = (state = initState, auction) => {
  return state;
};

export default authReducer;
